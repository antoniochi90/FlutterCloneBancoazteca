import 'package:flutter/material.dart';

class NominaScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Nómina'),
      ),
      body: Center(
        child: Text('Bienvenido a Nómina'),
      ),
    );
  }
}
