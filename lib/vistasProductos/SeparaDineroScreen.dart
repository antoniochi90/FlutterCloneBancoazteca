import 'package:flutter/material.dart';

class SepararDineroScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Separa tu dinero'),
      ),
      body: Center(
        child: Text('Bienvenido a Separa tu dinero'),
      ),
    );
  }
}
